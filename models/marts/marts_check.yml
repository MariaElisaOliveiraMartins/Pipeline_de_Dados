version: 2

models:
  - name: dim_geography
    columns:
      - name: geography_key
        tests:
          - unique
          - not_null
      - name: country_name
        tests:
          - not_null

  - name: fct_churn
    columns:
      - name: customer_id
        tests:
          - unique
          - not_null